---
phase: 01-ci
plan: "01-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
autonomous: true
requirements:
  - PACK-01
user_setup: []
must_haves:
  truths:
    - 仓库根目录存在 requirements.txt
    - 文件内为 PyPI 包名 requests、beautifulsoup4、lxml 且为 PEP 440 可解析的版本约束行
  artifacts:
    - requirements.txt
  key_links: []
---

<objective>
在仓库根目录新增 `requirements.txt`，声明与 `novel_downloader.py` 运行时 import 及 GitHub Actions 当前 `pip install` 一致的三元依赖，并加简短注释指明白同步 CI。

Purpose: 满足 PACK-01「声明式依赖文件」与 PROJECT 中可复现安装路径。  
Output: 单一新文件 `requirements.txt`（UTF-8）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-ci/01-CONTEXT.md
@novel_downloader.py
@.github/workflows/manual_download.yml
</context>

<threat_model>
| 威胁 | 严重度 | 本计划缓解 |
|------|--------|------------|
| 依赖与 CI/代码不一致 | 中 | 仅写入与 CONTEXT D-01 一致的三包及与现有 workflow 行语义相同的约束 |
| 非 PyPI 源 | 低 | 不配置额外 index，默认 PyPI |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 添加 requirements.txt</name>
  <files>requirements.txt</files>
  <read_first>novel_downloader.py, .planning/phases/01-ci/01-CONTEXT.md, .github/workflows/manual_download.yml</read_first>
  <action>在仓库根创建 `requirements.txt`（新文件）。内容须：1) 顶行或紧邻包行前用 `#` 注释说明「与 novel_downloader / manual_download CI 保持同步」；2) 三个包各一行，包名须严格为 `requests`、`beautifulsoup4`、`lxml`（与 pip 安装名一致，**不要用** `bs4` 作为包名行）；3) 每行使用 `>=` 加**具体**最低版本号：`requests>=2.25.0`、`beautifulsoup4>=4.9.0`、`lxml>=4.6.0`（与 CONTEXT 中 D-02「下限约束」一致，且为常见安全下限）。不添加未使用的包。</action>
  <verify>test -f requirements.txt &amp;&amp; wc -l requirements.txt</verify>
  <acceptance_criteria>
    - `python3 -c "t=open('requirements.txt').read(); assert 'requests' in t and '2.25.0' in t and t.count('requests')==1"` 退出码 0
    - `python3 -c "t=open('requirements.txt').read(); assert 'beautifulsoup4' in t and '4.9.0' in t"` 退出码 0
    - `python3 -c "t=open('requirements.txt').read(); assert 'lxml' in t and '4.6.0' in t"` 退出码 0
    - `python3 -c "t=open('requirements.txt').read().splitlines(); assert any(x.strip().startswith('#') for x in t)"` 退出码 0
  </acceptance_criteria>
  <done>三行约束行存在且可 pip 解析；注释说明同步 CI</done>
</task>

</tasks>

<verification>
- [ ] `python -m pip install --dry-run -r requirements.txt` 在本地（或 CI 逻辑上等价）不报错
- [ ] `grep -c -E '^(requests|beautifulsoup4|lxml)&gt;=' requirements.txt` 输出为 `3`
</verification>

<success_criteria>
- 新文件已提交且满足 acceptance_criteria
- 无额外无关依赖
</success_criteria>

<output>
完成后创建 `.planning/phases/01-ci/01-01-SUMMARY.md`（由执行阶段生成）。
</output>
