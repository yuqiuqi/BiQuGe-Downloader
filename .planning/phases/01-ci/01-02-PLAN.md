---
phase: 01-ci
plan: "01-02"
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - .github/workflows/manual_download.yml
autonomous: true
requirements:
  - PACK-02
user_setup: []
must_haves:
  truths:
    - Install dependencies 步骤使用 `pip install -r requirements.txt`，且不再依赖逐行列出三包的旧方式作为唯一安装手段
  artifacts:
    - .github/workflows/manual_download.yml
  key_links:
    - requirements.txt 与 workflow 安装命令一致
---

<objective>
将 `manual_download` workflow 的安装步骤改为从仓库根 `requirements.txt` 安装，与 01-01 及 CONTEXT D-04 一致，满足 PACK-02。

Purpose: CI 与本地「同一事实来源」声明一致。  
Output: 更新后的 `.github/workflows/manual_download.yml`。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ci/01-CONTEXT.md
@requirements.txt
@.github/workflows/manual_download.yml
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| Workflow 引用不存在的路径 | 中 | 仅在 01-01 合并后执行；`checkout` 后存在 `requirements.txt` |
| 使用非 PyPI 索引 | 低 | 不添加 `pip` 的 `--index-url` |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 对齐 CI 安装命令</name>
  <files>.github/workflows/manual_download.yml</files>
  <read_first>.github/workflows/manual_download.yml, requirements.txt, .planning/phases/01-ci/01-CONTEXT.md</read_first>
  <action>编辑 `Install dependencies` 步骤的 `run:` 块：保留 `python -m pip install --upgrade pip` 作为首行（CONTEXT 允许）。将原 `pip install requests beautifulsoup4 lxml` **整行替换**为 `pip install -r requirements.txt`（工作目录为仓库根，与 `actions/checkout` 一致）。不要添加第二套并行的逐包 install。若需注释，在替换行上方用 YAML `#` 写一句：与根目录 requirements.txt 同步。</action>
  <verify>test -f .github/workflows/manual_download.yml</verify>
  <acceptance_criteria>
    - `grep -n "pip install -r requirements.txt" .github/workflows/manual_download.yml` 返回至少一行匹配
    - `grep "pip install requests beautifulsoup4 lxml" .github/workflows/manual_download.yml` 退出码 1（旧行已不存在）
  </acceptance_criteria>
  <done>YAML 仍合法；仅通过 -r 安装运行时依赖</done>
</task>

</tasks>

<verification>
- [ ] `grep -q 'pip install -r requirements.txt' .github/workflows/manual_download.yml`
- [ ] 可选：本地 `act` 或仅语法检查通过（若无 act 则跳过）
</verification>

<success_criteria>
- PACK-02 的安装源与 `requirements.txt` 一致
- 无重复安装块
</success_criteria>

<output>
完成后创建 `01-02-SUMMARY.md`。
</output>
