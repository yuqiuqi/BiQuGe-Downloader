---
phase: 01-ci
plan: "01-03"
type: execute
wave: 3
depends_on:
  - "01-02"
files_modified:
  - README.md
autonomous: true
requirements:
  - PACK-01
  - PACK-02
user_setup: []
must_haves:
  truths:
    - README 中明确推荐通过 `requirements.txt` 安装（含 venv 与 `pip install -r` 示例命令）
    - README 说明与 GitHub Actions 使用同一依赖声明
  artifacts:
    - README.md
  key_links:
    - 与 requirements.txt 及 workflow 叙述一致
---

<objective>
更新 `README.md` 的依赖与安装说明：以「虚拟环境 + `pip install -r requirements.txt`」为推荐路径，保留「直接运行脚本可自举」为可选说明；并简短说明 Actions 与本地共用 `requirements.txt`。

Purpose: 满足 ROADMAP Phase 1 成功标准第 3 条与 CONTEXT D-06。  
Output: 更新后的 `README.md`。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/phases/01-ci/01-CONTEXT.md
@requirements.txt
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 文档与真实文件不一致 | 中 | 明确写文件名 `requirements.txt` 与仓库根路径 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 更新 README 依赖与安装章节</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/01-ci/01-CONTEXT.md</read_first>
  <action>在 `## 📦 依赖要求` 或其后新增小节「推荐安装（与 CI 一致）」：1) 使用三行左右 fenced bash 示例，包含 `python3 -m venv .venv`、`source .venv/bin/activate`（Windows 用注释一行说明 `.\.venv\Scripts\activate`）；2) `pip install -r requirements.txt`。保留原有「Python 3.6+」一句但在其后追加「与 GitHub Actions 一致时建议使用 Python 3.10」。3) 将原「脚本会自动安装」整段改为「**可选**：若不预先安装，脚本仍会尝试自动 pip 安装（见 novel_downloader.py）」。4) 在「方式一：本地运行」步骤中，在运行 `python novel_downloader.py` 之前增加一句：若已按上文创建 venv 并安装依赖，则直接运行。5) 在「方式三：GitHub Actions」中增加半句：工作流从仓库根 `requirements.txt` 安装依赖（与本地相同）。不删除免责声明。</action>
  <verify>wc -l README.md</verify>
  <acceptance_criteria>
    - `grep -F 'pip install -r requirements.txt' README.md` 退出码 0
    - `grep -F 'requirements.txt' README.md` 至少匹配 2 次（依赖章节 + Actions 或重复提及）
    - `grep -F '3.10' README.md` 退出码 0
  </acceptance_criteria>
  <done>新用户可仅凭 README 完成 venv + 依赖安装；知悉与 CI 同源</done>
</task>

</tasks>

<verification>
- [ ] 通读 README，无「仅能通过自动 pip」为唯一路径的歧义
</verification>

<success_criteria>
- PACK-01 / PACK-02 的文档侧验收与 ROADMAP 成功标准 3 对齐
</success_criteria>

<output>
完成后创建 `01-03-SUMMARY.md`。
</output>
