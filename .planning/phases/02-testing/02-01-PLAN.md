---
phase: 02-testing
plan: "02-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements-dev.txt
  - url_input.py
  - tests/test_url_input.py
  - novel_downloader.py
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - 根目录 `pytest` 无参退出码 0
    - `url_input` 可单独 import，不触发子进程与网络
  artifacts:
    - requirements-dev.txt
    - url_input.py
    - tests/test_url_input.py
  key_links:
    - `novel_downloader` 的 `if __name__` 经 `url_input` 再生成下载 URL
---

<objective>
新增**无自举、仅标准库**的 `url_input.py`，抽离原 `__main__` 中的书号/URL 归一化；增加 `requirements-dev.txt` 与 `tests/test_url_input.py`（表驱动、无网）；在 `novel_downloader.py` 的入口块中改为调用 `normalize_target_url`，满足 02-CONTEXT D-01～D-07 与 **TEST-01/TEST-02**。

Output: 上述文件创建或修改，本地 `pytest` 可通过。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-testing/02-CONTEXT.md
@novel_downloader.py
@requirements.txt
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 测试 import 主脚本 | 中 | 单测只 import `url_input` 与 `pytest` 收集路径 |
| 行为偏离旧入口 | 中 | 表驱动用例覆盖 `3953`、全 URL、缺 `http` 的域名行 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 添加 requirements-dev.txt</name>
  <files>requirements-dev.txt</files>
  <read_first>.planning/phases/02-testing/02-CONTEXT.md</read_first>
  <action>新建 `requirements-dev.txt`：第 1 行起用 `#` 写「仅开发/CI 测试，非运行 novel_downloader 必装」；第一组**非注释**行写 `pytest>=7.0.0`。不得加入 coverage/tox。  
</action>
  <verify>head -5 requirements-dev.txt</verify>
  <acceptance_criteria>
    - `python3 -c "t=open('requirements-dev.txt').read(); assert 'pytest' in t and '7' in t"` 退出码 0
    - `python3 -c "assert any(l.strip().startswith('#') for l in open('requirements-dev.txt'))"` 退出码 0
  </acceptance_criteria>
  <done>dev 依赖可单独安装</done>
</task>

<task type="auto">
  <name>T2: 实现 url_input 纯函数</name>
  <files>url_input.py</files>
  <read_first>novel_downloader.py（仅 `if __name__` 中 URL 相关分支）</read_first>
  <action>新建 `url_input.py`，**仅**使用标准库。提供函数 `normalize_target_url(s: str) -> str`：对入参**假定**为已 `strip` 且非空（调用方在空串时不得调用，与现逻辑一致）：（1）若 `s.isdigit()` 为真，返回 `f"https://m.bqg92.com/book/{s}/"`；（2）否则若 `not s.startswith("http")`，返回 `"https://" + s`；（3）否则原样返回 `s`。不发起网络、无 subprocess。可选模块常量化默认主机段以便阅读。加简短模块 docstring 说明与 CLI 原行为一致。  
</action>
  <verify>python3 -c "import url_input; assert url_input.normalize_target_url('3953').endswith('3953/')"</verify>
  <acceptance_criteria>
    - 文件内存在字符串 `m.bqg92.com` 与 `def normalize_target_url`
    - `python3 -c "import url_input; import inspect; assert not any('requests' in l for l in open('url_input.py'))"`  或等价检查无第三方 import
  </acceptance_criteria>
  <done>纯逻辑与 CONTEXT D-02 一致</done>
</task>

<task type="auto">
  <name>T3: 添加 tests/test_url_input.py</name>
  <files>tests/test_url_input.py</files>
  <read_first>url_input.py, .planning/phases/02-testing/02-CONTEXT.md</read_first>
  <action>新建 `tests/test_url_input.py`。使用 `pytest.mark.parametrize` 或等价，至少 4 组用例，**必须**包含：`3953` → 以 `https://m.bqg92.com/book/3953/` 结尾、完整 `https://example.com/book/1/` 不变、`m.bqg92.com/foo` 类输入补 `https://`、全数字外的普通 URL 行为。用 `from url_input import normalize_target_url`。**禁止** `import novel_downloader`。  
</action>
  <verify>python3 -m pytest tests/test_url_input.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_url_input.py -q` 退出码 0
    - `grep -R "novel_downloader" tests/` 在仓库内退出码 1（无引用）
  </acceptance_criteria>
  <done>TEST-02（无网单测）落地</done>
</task>

<task type="auto">
  <name>T4: 接入 novel_downloader 入口</name>
  <files>novel_downloader.py</files>
  <read_first>novel_downloader.py（尾部 `if __name__` 块）, url_input.py</read_first>
  <action>在 `if __name__ == "__main__":` 块**内部**、在已得到非空 `input_str` 并 `strip` 为 `target_url` 之后，将原先对 `isdigit` / `startwith('http')` 的 if/elif **替换**为单次赋值：`target_url = normalize_target_url(target_url)`（在 `if not input_str` 处理之后、构造 `NovelDownloader` 之前）。在 `if __name__` 块顶处增加 `from url_input import normalize_target_url`（仅主入口执行，不提升模块级 import 以免运行 `python -m novel_downloader` 外路径时改变行为——保持 import 在 `if __name__` 内）。**保留**纯数字时原有的 `print(f"检测到输入为ID，已自动补全为: {target_url}")`：可在 `normalize` 后若原 `input_str.strip().isdigit()` 则 print。与旧行为用户可见串一致。  
</action>
  <verify>python3 -c "print('syntax ok')"</verify>
  <acceptance_criteria>
    - `grep -n "normalize_target_url" novel_downloader.py` 至少一行
    - `python3 -m py_compile novel_downloader.py url_input.py` 退出码 0
  </acceptance_criteria>
  <done>入口经 url_input 与现逻辑对齐</done>
</task>

<task type="auto">
  <name>T5: 根目录全量 pytest</name>
  <files></files>
  <read_first>requirements-dev.txt, requirements.txt</read_first>
  <action>在**已** `pip install -r requirements.txt -r requirements-dev.txt` 的环境中执行根目录 `pytest`（无参），应收集 `tests/` 下测试并全部通过。  
</action>
  <verify>cd / 仓库根; pytest 2&gt;&amp;1 | tail -3</verify>
  <acceptance_criteria>
    - 命令 `pytest` 在仓库根退出码 0
  </acceptance_criteria>
  <done>TEST-01 smoke 通过</done>
</task>

</tasks>

<verification>
- [ ] `pytest` 与 `pytest -q` 均退出 0
- [ ] 未在 `tests/` 中 import `novel_downloader`
</verification>

<success_criteria>
- TEST-01 / TEST-02 在代码与命令层均可验证
</success_criteria>

<output>
执行后写 `02-01-SUMMARY.md`。
</output>
