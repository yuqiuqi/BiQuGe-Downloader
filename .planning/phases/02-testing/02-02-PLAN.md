---
phase: 02-testing
plan: "02-02"
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - TEST-01
user_setup: []
must_haves:
  truths:
    - README 含「如何安装 dev 依赖并运行 pytest」的可复制命令
  artifacts:
    - README.md
  key_links: []
---

<objective>
在 **02-01** 已具备可跑测试的前提下，补充 **README** 中「开发 / 运行测试」小节，使协作者可复现 `pip install -r requirements.txt -r requirements-dev.txt` 与 `pytest`；**可选**添加最小 `pyproject.toml` 中 `[tool.pytest.ini_options]`（`testpaths = ["tests"]`）以固定发现路径（若仓库尚无 `pyproject.toml` 则创建；若已存在则仅追加/合并该节不破坏他节）。

满足 ROADMAP 中「02-02」的文档/可选扩展示意与 CONTEXT D-11；**不**强制 HTML fixture（D-08）。

Output: 更新 `README.md`；可选 `pyproject.toml`。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@README.md
@.planning/phases/02-testing/02-CONTEXT.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 文档与真实命令不一致 | 中 | 与 02-01 中实际文件名、命令字面值一致 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: README 增加运行测试</name>
  <files>README.md</files>
  <read_first>README.md, requirements-dev.txt</read_first>
  <action>在 `README.md` 增加独立小节（建议标题「## 开发（运行测试）」或等效），**必须**包含三元素：（1）说明需同时安装 `requirements.txt` 与 `requirements-dev.txt`；（2）**可复制**的 bash 代码块：`python3 -m venv .venv` + `source .venv/bin/activate` + `pip install -r requirements.txt -r requirements-dev.txt`（Windows 可一行注释 `.\.venv\Scripts\activate`）；（3）`pytest` 与 `pytest -q` 作为等价的运行方式说明。不要声称「仅 pytest 无需 runtime 依赖」——因测试不 import 主脚本的 `requests` 前自举，但 `requirements-dev` 与文档路径须清晰。  
</action>
  <verify>wc -l README.md</verify>
  <acceptance_criteria>
    - `grep -F 'requirements-dev.txt' README.md` 退出码 0
    - `grep -F 'pytest' README.md` 退出码 0
    - `grep -F 'pip install' README.md` 退出码 0
  </acceptance_criteria>
  <done>TEST-01 的文档可复现性增强</done>
</task>

<task type="auto">
  <name>T2: 可选 pyproject 中 pytest 配置</name>
  <files>pyproject.toml</files>
  <read_first>02-CONTEXT（Claude's Discretion 一条）</read_first>
  <action>若**不存在** `pyproject.toml`：创建最小 TOML，仅含 `[tool.pytest.ini_options]`，`testpaths = ["tests"]`（可含 `pythonpath = ["."]` 若需从根 import `url_input` ——当前测试从仓库根已可发现 `url_input`，则通常仅需 `testpaths`）。若**已存在** `pyproject.toml`：在**不删除**他节的前提下合并 `pytest` 工具块。若用户项目拒绝 pyproject，可**跳过**本任务并在 SUMMARY 写「已跳过，理由：…」。  
</action>
  <verify>test -f README.md</verify>
  <acceptance_criteria>
    - 若执行后**新建**了 `pyproject.toml`，则其含 `[tool.pytest.ini_options]` 与 `testpaths` 指向 `tests`；若项目拒绝新增该文件，则在 `02-02-SUMMARY.md` 的 Deviations 中写明原因且 README 中测试命令已足够
  </acceptance_criteria>
  <done>pytest 发现路径有文档或显式 config</done>
</task>

</tasks>

<verification>
- [ ] 新贡献者仅按 README 可跑通 `pytest`
</verification>

<success_criteria>
- TEST-01 的「一条命令」在文档中显式
</success_criteria>

<output>
执行后写 `02-02-SUMMARY.md`。
</output>
