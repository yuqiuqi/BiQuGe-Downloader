---
phase: 02-testing
plan: "02-03"
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - .github/workflows/manual_download.yml
autonomous: true
requirements:
  - TEST-01
  - TEST-02
user_setup: []
must_haves:
  truths:
    - 同一 workflow 中 `test` job 在 `download` 之前成功执行 `pytest`
  artifacts:
    - .github/workflows/manual_download.yml
  key_links:
    - 安装顺序与 02-CONTEXT D-9 一致
---

<objective>
在 `.github/workflows/manual_download.yml` 增加独立 **`test` job**（Python 3.10、`ubuntu-latest`），按顺序 `checkout` → `setup-python` → 安装 `requirements.txt` 与 `requirements-dev.txt` → 根目录 `pytest`；为现有 **`download` job** 增加 **`needs: [test]`**，使下载仅在测试通过之后运行。满足 02-CONTEXT D-09、D-10 与 **TEST-01/TEST-02**（CI 上执行与本地相同测试套）。

Output: 更新后的 workflow YAML。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
@$HOME/.cursor/get-shit-done/templates/summary.md
</execution_context>

<context>
@.github/workflows/manual_download.yml
@.planning/phases/02-testing/02-CONTEXT.md
@requirements.txt
@requirements-dev.txt
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| job id 与 needs 名不一致 | 中 | 使用 YAML 锚点或统一命名 `test` / `download` |
| 未装 dev 依赖 | 中 | 显式两文件 pip install |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 增加 test job</name>
  <files>.github/workflows/manual_download.yml</files>
  <read_first>.github/workflows/manual_download.yml, .planning/phases/02-testing/02-CONTEXT.md</read_first>
  <action>在 `jobs:` 下**新增**顶级键 `test`（与 `download` 平级，勿嵌套在 download 内）。`test` 配置：`runs-on: ubuntu-latest`；`steps`：① `actions/checkout@v3`（与 download 同版本可）；② `actions/setup-python@v4` 且 `python-version: '3.10'`；③ `name: Install dependencies` 的 `run` 块包含：`python -m pip install --upgrade pip`、`pip install -r requirements.txt`、`pip install -r requirements-dev.txt`（可附注释与 Phase 1 风格一致）；④ `name: Run tests` 的 `run: pytest`（或 `python -m pytest` 二选一并与 README 终稿一致，**建议 `pytest`）。**不要**在 test job 里跑 `python novel_downloader.py`。  
</action>
  <verify>python3 -c "import sys; f=open('.github/workflows/manual_download.yml'); print('ok')"</verify>
  <acceptance_criteria>
    - 文件中存在 `^  test:` 或 `^test:` 作为 jobs 下键（`grep` 可识别 `test:` 作为 job id）
    - `grep -F "pytest" .github/workflows/manual_download.yml` 退出码 0
    - `grep -F "requirements-dev.txt" .github/workflows/manual_download.yml` 退出码 0
  </acceptance_criteria>
  <done>CI 能执行 pytest</done>
</task>

<task type="auto">
  <name>T2: download 依赖 test</name>
  <files>.github/workflows/manual_download.yml</files>
  <read_first>.github/workflows/manual_download.yml</read_first>
  <action>在 **`download` job**（保留原名）下增加 **`needs: [test]`**（若 job 键为 `test`；若你命名 `Tests` 等则与 needs 中 id 严格一致）。确保 YAML 缩进为 2 空格；`download` 的其余 `steps` 与 `on:` 不删除。  
</action>
  <verify>grep -A2 "download:" .github/workflows/manual_download.yml | head -5</verify>
  <acceptance_criteria>
    - `grep "needs:.*test" .github/workflows/manual_download.yml` 在 `download` 段下可被发现（或 `needs:\n` 下一行 ` - test`）
  </acceptance_criteria>
  <done>D-10 满足</done>
</task>

</tasks>

<verification>
- [ ] GitHub **Actions 语法**有效（可本地用 `actionlint` 若存在，否则手检缩进）
- [ ] 任意 PR 上 `test` 先于 `download` 出现（文档层说明即可）
</verification>

<success_criteria>
- ROADMAP Phase 2 成功标准 2 满足：同一 workflow 内跑与本地一致的 `pytest` 且先于下载
</success_criteria>

<output>
执行后写 `02-03-SUMMARY.md`。
</output>
