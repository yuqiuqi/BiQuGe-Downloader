# UAT：随机全本下载（亲妈被认回豪门后，id=3953）

**日期:** 2026-04-23  
**环境:** 本地，命令 `python3 novel_downloader.py 3953`（apibi API 模式）  
**预期章节数:** 113（与 `/api/booklist` 的 `list` 长度一致）

## 通过项

1. **完整性**
   - 下载进度 113/113，无「下载失败」「无法解析此章节」类占位串写入正文。
   - 正文末章含「全文完结」类结束语，与连载状态「已经完本」一致。
2. **编码**
   - 文件为合法 UTF-8，无 U+FFFD 替换符。
   - 无 CRLF 混用（纯 LF），无可打印控制符污染。
3. **结构**
   - 章节分隔线（20 个 `-`）共 113 条，与章数一致；章序与 API 目录顺序一致。

## 问题与证据（建议列入下一期）

### P1：API 正文中含站点推广/水印串（非乱码，但影响可读与排版）

- **现象:** `txt` 字段在句中、句尾大量插入 `bqfun ⊕cc` 片段；另有「顶点小说」「新顶点小说手机版阅读网址」等行。
- **粗计量（该次全本）:** 约 88 处 `bqfun ⊕cc` 模式；含「顶点小说」的片段约 93 次量级。
- **根因:** 源站/API 在正文内嵌营销与水印，非落盘 UTF-8 错误。
- **建议:** 在 `fetch_chapter_text_api` 或写入前增加**可维护的清洗**（白名单/正则子串；注意勿删合法正文）；可选 `--no-strip-ads` 保留原貌。

### P2：章首/文首多余空行

- **现象:** 生成文件开头存在空行后再出现「第1章…」。
- **建议:** 首次写入时去掉文件头连续 `\n`，或章块拼接时统一去秃头空白。

### P3：章末可夹带非正文链接行（源数据）

- **现象:** 末章或部分章末出现如 `/read/29475/`、站点 URL 类残留（来自源 `txt`）。
- **建议:** 与 P1 一并纳入「尾部垃圾行」规则或二次过滤。

## 样例（末章截断，供复查）

末段仍可见内嵌 `bqfun ⊕cc` 与站点宣传行；完结标记「————全文完结————」存在，**故事正文层面完整**。

---

*测试产出的大体积 TXT 已从工作区删除，避免误入版本库。如需复测可再次运行同一命令。*
