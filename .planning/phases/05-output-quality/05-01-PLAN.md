---
phase: 05-output-quality
plan: "05-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - text_clean.py
  - novel_downloader.py
autonomous: true
requirements:
  - TXT-01
  - TXT-02
user_setup: []
must_haves:
  truths:
    - 新增 `text_clean` 模块且可被纯 import（不触发 novel_downloader 顶层 pip 自举）
    - apibi 与 HTML 两路径返回读者可见正文前均经同一清洗入口（raw 时跳过规则）
  artifacts:
    - text_clean.py
  key_links:
    - `.planning/phases/05-output-quality/05-CONTEXT.md` D-01～D-06、D-03
---

<objective>
实现 `text_clean.py`（数据驱动规则 + `clean_chapter_text(text, raw: bool)`），并在 `NovelDownloader` 中增加 `_raw_text: bool`（从环境变量 `BQUGE_RAW_TEXT` 真值读取，见任务），在 **`get_chapter_content`** 的 apibi 与 HTML 两分支在**返回前**对正文字符串调用清洗（raw 为 True 时跳过除 UTF-8 无害处理外的规则）。不修改 Phase 6/7 范围。

Output: 代码可编译；行为与 CONTEXT 一致。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-output-quality/05-CONTEXT.md
@novel_downloader.py
@bqg_api.py
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 误删合法正文 | 高 | 行级/保守正则；单测覆盖「对话含类似子串」 |
| import 触发自举 | 中 | `text_clean` 零第三方；测网不 import `novel_downloader` |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 新建 text_clean.py</name>
  <files>text_clean.py</files>
  <read_first>.planning/phases/05-output-quality/05-CONTEXT.md,.planning/phases/03-content-quality/03-UAT-DOWNLOAD-REPORT.md</read_first>
  <action>
1. 模块内定义**可迭代**的「规则」结构（如元组列表）：每条含 `name`（说明）、`pattern`（编译后的 `re.Pattern` 或 `None`）与 `repl` 或行过滤函数；**禁止**在 `novel_downloader` 内写裸字符串除 import 常量外。  
2. 首版至少覆盖 UAT：**行**或子串移除/替换 `bqfun` 与 `⊕`/`cc` 组合噪声；**整行**匹配为纯推广时删除含「顶点小说」「新顶点小说」「阅读网址」的典型行；**整行**仅为 `/read/\d+/` 形式的路径行。  
3. 实现 `def clean_chapter_text(text: str, raw: bool = False) -> str`：`raw is True` 时**不**应用上述营销规则，仅可做 `text.replace("\r\n","\n")` 等无损规范化（若实现则单测断言 raw 与输入除换行外一致）。  
4. 若 `raw is False`，依次应用规则后 `strip()` 章节末多余空行（连续换行压为最多两个 `\n` 或单段落间一个空行，与现有章内风格一致）。  
  </action>
  <verify>python3 -m py_compile text_clean.py</verify>
  <acceptance_criteria>
    - `grep -q "def clean_chapter_text" text_clean.py`
    - `python3 -c "import text_clean; s='a bqfun ⊕cc b'; assert '⊕' not in text_clean.clean_chapter_text(s, False)"`
  </acceptance_criteria>
  <done>清洗纯函数可 import</done>
</task>

<task type="auto">
  <name>T2: NovelDownloader 接入清洗与 raw 开关</name>
  <files>novel_downloader.py</files>
  <read_first>novel_downloader.py（`__init__`、`get_chapter_content` 全文）,text_clean.py</read_first>
  <action>
1. 在 `__init__(self, target_url, raw_text=None)`：`raw_text is None` 时读取 `os.environ.get("BQUGE_RAW_TEXT","").strip().lower()` 属于 `("1","true","yes","on")` 则 `self._raw_text=True`，否则 `False`；若调用方显式传 `bool` 则以其为准。  
2. 在 `get_chapter_content`：apibi 分支在 `fetch_chapter_text_api` 得到非 `None` 的 `text` 后执行 `text = clean_chapter_text(text, self._raw_text)` 再 `return`。HTML 分支在拼好 `full_content` 后、在 `return full_content if full_content else ...` 之前对 `full_content` 同样调用 `clean_chapter_text(..., self._raw_text)`。错误路径（无法解析等短句）**不**经过营销清洗或仅当长度大于某阈值时再洗（实现任择一：推荐**不**清洗失败提示串，避免误改）。  
3. 文件头 `from text_clean import clean_chapter_text`（放在 requests 等之后、类定义之前即可，避免与现有 import 风格冲突）。  
  </action>
  <verify>python3 -m py_compile novel_downloader.py text_clean.py</verify>
  <acceptance_criteria>
    - `grep -q "clean_chapter_text" novel_downloader.py`
    - `grep -q "_raw_text" novel_downloader.py`
  </acceptance_criteria>
  <done>双路径统一出口</done>
</task>

</tasks>

<verification>
- `python3 -m py_compile text_clean.py novel_downloader.py` 退出码 0
- 手工：`python3 -c "from text_clean import clean_chapter_text; print(clean_chapter_text('test bqfun ⊕cc\\n', False))"` 输出不含 `⊕`
</verification>
