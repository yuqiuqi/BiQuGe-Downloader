---
phase: 05-output-quality
plan: "05-02"
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - novel_downloader.py
autonomous: true
requirements:
  - TXT-02
user_setup: []
must_haves:
  truths:
    - 合并文件首字符不为「先空两行再第一章」类 UAT P2 问题（允许首章前单空行或零空行，与实现一致且在计划验收中写明）
    - `__main__` 支持 `--raw-text` 或解析后设置 `raw_text=True` 传入 `NovelDownloader`
  artifacts:
    - novel_downloader.py（同文件二次修改）
---

<objective>
修正 `run()` 顺序写盘时的**文首与章间**空白（05-CONTEXT D-07、D-08）：首章写入前不写前导多余 `\n`；章与章之间避免 `\\n\\n\\n` 三连以上。为 CLI 增加 `--raw-text`（可选位置：在 URL/ID 参数前或后，用简单 `sys.argv` 扫描实现即可），创建 `NovelDownloader(..., raw_text=True)` 当该 flag 出现。

Output: 落盘版式符合 CONTEXT；默认与无 flag 时行为与 Phase 5 之前相比仅多「清洗」而非多「空行」。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-output-quality/05-CONTEXT.md
@novel_downloader.py（`run` 与 `if __name__`）
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 破坏 Actions 传参 | 中 | 仅多一个可选 flag；`python novel_downloader.py 3953` 仍可用 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: run() 写盘版式</name>
  <files>novel_downloader.py</files>
  <read_first>novel_downloader.py（`run` 方法 `with open` 循环）,.planning/phases/05-output-quality/05-CONTEXT.md</read_first>
  <action>
1. 将 `for i in range(total_chapters):` 循环内首次写入（`i==0`）改为：`f.write(f"\n\n{title}\n")` 改为**无**前导双换行：首章使用 `f.write(f"{title}\n")` 或 `f.write(f"\n{title}\n")` **二选一**，以**文件首行是章节标题**为准（不要先输出空行）。自 `i>=1` 起保持章间分隔，仍用 `\n\n` 再接标题。  
2. 对 `content` 写入后若下一章存在，避免 `content` 尾部与下一章标题之间出现 4 个以上连续换行：可在写入 `content` 后 `rstrip()` 再写单一 `\n`，或写入章后 `content` 经 `text_clean` 已有规范——**若** `clean_chapter_text` 已 trim 章末，此处仅协调与 `f.write("\n\n")` 的衔接。  
3. `-" * 20` 与 `-" * 30` 若并存，保持与现有一致或统一为 20（若 CONTEXT 未要求改分隔线长度则**不**改数字仅改空白）。当前代码为 20，保持 20。  
  </action>
  <verify>grep -n "for i in range(total_chapters)" -A25 novel_downloader.py</verify>
  <acceptance_criteria>
    - `novel_downloader.py` 中 `run` 内首章写入路径：首写不得为 `f.write("\\n\\n"` 接标题（`i==0` 分支）
    - `grep -c "--------------------" novel_downloader.py` ≥ 1
  </acceptance_criteria>
  <done>UAT P2 类文首空行消除</done>
</task>

<task type="auto">
  <name>T2: CLI --raw-text</name>
  <files>novel_downloader.py</files>
  <read_first>novel_downloader.py（`if __name__`）</read_first>
  <action>
在 `if __name__ == "__main__":` 中、构造 `NovelDownloader` 之前：若 `sys.argv[1:]` 中含字符串 `"--raw-text"`，则从 argv 列表中**移除**该元素，并设 `raw_text=True` 传入 `NovelDownloader(target_url, raw_text=True)`；否则 `NovelDownloader(target_url)`。**保证** 纯数字书号作第一个参数时仍可用：例如 `python novel_downloader.py 3953` 不变；`python novel_downloader.py --raw-text 3953` 与 `python novel_downloader.py 3953 --raw-text` 均应被支持（需扫描全 argv 去 flag 后再取 URL）。  
  </action>
  <verify>python3 -m py_compile novel_downloader.py</verify>
  <acceptance_criteria>
    - `grep -q "raw_text" novel_downloader.py` 在 `if __name__` 块附近
    - `grep -q "\\-\\-raw-text" novel_downloader.py` 或 "raw-text" 字符串存在
  </acceptance_criteria>
  <done>CLI 与 env 双通道（env 在 05-01）对齐 README 预留</done>
</task>

</tasks>

<verification>
- `python3 -c "import sys; sys.argv=['novel','3953','--raw-text']"` 不执行；代之以代码审阅 grep
</verification>
