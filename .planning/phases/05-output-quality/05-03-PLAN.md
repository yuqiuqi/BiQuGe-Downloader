---
phase: 05-output-quality
plan: "05-03"
type: execute
wave: 3
depends_on:
  - "05-01"
  - "05-02"
files_modified:
  - tests/test_text_clean.py
  - README.md
autonomous: true
requirements:
  - TXT-01
  - TXT-02
user_setup: []
must_haves:
  truths:
    - `pytest` 在仓库根退出 0
    - `tests/` 下仍无 `import novel_downloader`（grep）
  artifacts:
    - tests/test_text_clean.py
---

<objective>
为 `text_clean` 增加 **pytest** 覆盖（合成字符串，含 UAT 匿名化片段：含 `bqfun ⊕cc`、独立推广行、raw 模式恒等）；**README** 增加「`--raw-text` / `BQUGE_RAW_TEXT`」与清洗默认开启的简短说明。不引入网络测试。

Output: 根目录 `pytest` 全绿；文档可指导 Actions/本地用户。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/05-output-quality/05-CONTEXT.md
@README.md
@text_clean.py（待创建后）
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 测网 import 主模块 | 中 | 仅 `import text_clean` |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: tests/test_text_clean.py</name>
  <files>tests/test_text_clean.py</files>
  <read_first>text_clean.py,tests/test_bqg_api.py</read_first>
  <action>
新建 `tests/test_text_clean.py`：`import pytest` 与 `from text_clean import clean_chapter_text`。用 `parametrize` 至少 5 组： (1) 含 `bqfun ⊕cc` 正文字符串经 `raw=False` 后子串不再出现； (2) `raw=True` 时与输入在替换 `\\r\\n` 后一致（或文档化例外）； (3) 独立一行「…顶点小说…」被删除； (4) 独立一行 `/read/12345/` 被删除； (5) 正常对话句含「顶点」**非整行**时不删除（合成短句验证不误杀）。**不得** `import novel_downloader`。  
  </action>
  <verify>cd 仓库根 && pytest tests/test_text_clean.py -q</verify>
  <acceptance_criteria>
    - `pytest tests/test_text_clean.py -q` 退出码 0
    - `! grep -R "novel_downloader" tests/ --include='*.py'` 或 `grep -R "novel_downloader" tests/ || test $? -ne 0` 逻辑上等价：tests 中无该 import
  </acceptance_criteria>
  <done>TXT-01/02 测试化</done>
</task>

<task type="auto">
  <name>T2: README 补充</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
在「使用方法」或「开发」附近新增短段落 **3～8 行**：说明默认会清洗 apibi/HTML 正文中的常见推广水印；关闭方式：`--raw-text` 与 `export BQUGE_RAW_TEXT=1`（与实现一致）。指向 UAT/规划非必须。  
  </action>
  <verify>grep -n "raw" README.md | head -5</verify>
  <acceptance_criteria>
    - `grep -i "BQUGE_RAW" README.md` 或 `grep -i "raw.text" README.md` 匹配成功
  </acceptance_criteria>
  <done>用户可知情选择 raw</done>
</task>

<task type="auto">
  <name>T3: 全量 pytest 回归</name>
  <files></files>
  <read_first>pyproject.toml,requirements-dev.txt</read_first>
  <action>在已安装依赖的环境中运行仓库根 `pytest`（无参），须全部通过。  
  </action>
  <verify>pytest -q 2>&1 | tail -5</verify>
  <acceptance_criteria>
    - `pytest` 在仓库根退出码 0
  </acceptance_criteria>
  <done>不破坏 Phase 2 测试基线</done>
</task>

</tasks>

<verification>
- 根目录 `pytest` 退出 0
</verification>
