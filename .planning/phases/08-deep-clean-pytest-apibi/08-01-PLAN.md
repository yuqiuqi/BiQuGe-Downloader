---
phase: 08-deep-clean-pytest-apibi
plan: "08-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - text_clean.py
  - tests/test_text_clean.py
autonomous: true
requirements:
  - CLEAN-01
user_setup: []
must_haves:
  truths:
    - 换行规范化之后、`clean_chapter_text` 对 default 与 raw 两路径均移除 U+FEFF，且不删其它 Unicode 标量
    - 短错误提示分支在 FEFF 去除后行为与 Phase 5 一致（不重洗营销）
  artifacts:
    - text_clean.py（含私有或模块级 `_strip_feff` 等，任择）
---

<objective>
实现 **CLEAN-01**：在 `clean_chapter_text` 内于 `_normalize_newlines` 之后、**任何**分支特判之前，统一执行 **U+FEFF 全串剥离**（见 `08-CONTEXT.md` D-01～D-03）。补充 `tests/test_text_clean.py`：**含 FEFF 的合成串**在 `raw=False` 与 `raw=True` 下均无 `\ufeff`。

Output: `python -m py_compile text_clean.py`；`pytest tests/test_text_clean.py -q` 通过本 plan 新增用例。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-deep-clean-pytest-apibi/08-CONTEXT.md
@text_clean.py
@tests/test_text_clean.py
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 误删合法 Unicode | 高 | 仅移除 U+FEFF，不碰其它码点；单测断言正常中文保留 |
| raw 语义被用户误解 | 中 | Wave 2 README 一句说明；本 plan 在 docstring 一行注记 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 实现 FEFF 剥离</name>
  <files>text_clean.py</files>
  <read_first>text_clean.py,.planning/phases/08-deep-clean-pytest-apibi/08-CONTEXT.md</read_first>
  <action>
1. 在 `clean_chapter_text` 中，紧跟 `_normalize_newlines(text)` 赋值后，对 `t` 执行 `t = t.replace("\ufeff", "")`（或抽出 `_strip_u200b_feff` 仅处理 FEFF，**勿**删除 U+200B 等除非 CONTEXT 后续扩展）。  
2. 确保 **raw 早退** 与 **短错误提示早退** 均在 **FEFF 已去除** 的 `t` 上判断/返回（与 D-03 一致）。  
3. 在模块或 `clean_chapter_text` docstring 中增加 **一行**：raw 模式仍会去 FEFF 与换行统一。  
  </action>
  <verify>python3 -m py_compile text_clean.py</verify>
  <acceptance_criteria>
    - `grep -q "ufeff\|FEFF" text_clean.py` 或代码中存在 `replace("\\ufeff"` 等价
    - `python3 -c "from text_clean import clean_chapter_text; s='\\ufeffab'; assert '\\ufeff' not in clean_chapter_text(s, True) and '\\ufeff' not in clean_chapter_text(s, False)"`
  </acceptance_criteria>
  <done>FEFF 逻辑落地</done>
</task>

<task type="auto">
  <name>T2: 单测 FEFF + raw</name>
  <files>tests/test_text_clean.py</files>
  <read_first>tests/test_text_clean.py,text_clean.py</read_first>
  <action>
新增至少：`(1)` 默认清洗含 `\ufeff` 的短正文；(2) `raw=True` 时仍无 `\ufeff` 但保留营销串（可构造含 `jqxs` 的串若与现有测试冲突则仅用 FEFF+纯中文）。命名清晰如 `test_strips_feff_default_and_raw`。  
  </action>
  <verify>cd 仓库根 && pytest tests/test_text_clean.py -q</verify>
  <acceptance_criteria>
    - 新增用例执行通过
    - `tests/` 仍无 `import novel_downloader`
  </acceptance_criteria>
  <done>FEFF 回归锁定</done>
</task>

</tasks>

<verification>
- `pytest tests/test_text_clean.py -q` 退出 0
- 手工：含 `\ufeff` 的 apibi 风格合成串经 `clean_chapter_text` 后 `"\ufeff" not in result`
</verification>
