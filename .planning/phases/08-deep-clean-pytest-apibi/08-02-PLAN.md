---
phase: 08-deep-clean-pytest-apibi
plan: "08-02"
type: execute
wave: 2
depends_on:
  - "08-01"
files_modified:
  - text_clean.py
  - tests/test_text_clean.py
  - README.md
autonomous: true
requirements:
  - CLEAN-02
user_setup: []
must_haves:
  truths:
    - 非 raw 时合成用例中代表 E2E 的 `jqxs ⊙cc`、`gctxt点cc` 类片段被去除或显著削弱（单测锁定具体断言）
    - 至少一条「叙事句含顶点/类似符号但非营销行」保持不全文删除
  artifacts:
    - README.md 在「正文清洗」段补一句 raw 与 FEFF
---

<objective>
实现 **CLEAN-02**：在 `text_clean` 的**数据驱动**行内规则表中追加 E2E 已证 apibi 水印族（`jqxs`+`⊙`+`cc`、`gctxt`+`点`+`cc` 及全半角/空白变体，见 `08-CONTEXT` D-04）。扩展 `tests/test_text_clean.py` 合成黄金串；**README** 在正文清洗段补 **一句**：`--raw-text` / `BQUGE_RAW_TEXT` 下仍做换行统一与 **U+FEFF 去除**，不删行内广告规则。

不跑必须联网的测试；不 `import novel_downloader`。

Output: 根目录 `pytest` 全绿。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/08-deep-clean-pytest-apibi/08-CONTEXT.md
@.planning/phases/999.1-apibi-watermark-bom/E2E-2026-04-23.md
@text_clean.py
@tests/test_text_clean.py
@README.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 误删作者刻意使用的 ⊙/点cc | 高 | 锚定 `jqxs`/`gctxt` 固定串；单测含反例句 |
| 正则在 Unicode 上灾难性回溯 | 中 | 保持模式简短；必要时用固定子串 + 小范围 `\s*` |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 扩展行内水印表</name>
  <files>text_clean.py</files>
  <read_first>text_clean.py,.planning/phases/08-deep-clean-pytest-apibi/08-CONTEXT.md,.planning/phases/999.1-apibi-watermark-bom/E2E-2026-04-23.md</read_first>
  <action>
1. 向 `_INLINE_NOISE`（或等价的编译表）追加若干 `(pattern, repl)` 项，**至少** 覆盖 E2E 与 CONTEXT 点名的 `jqxs`/`gctxt` 组合；`IGNORECASE` 用于拉丁字母段。  
2. 在注释中按「apibi 源站 2026-04 E2E」标注，便于以后审计。  
3. 保持与 Wave 1 的 **FEFF 步** 顺序：先 norm → FEFF → 再 `_apply_inline_replacements`。  
4. `raw=True` 路径**不**应用新增营销替换（与 Phase 5 一致），仅 norm + FEFF。  
  </action>
  <verify>python3 -m py_compile text_clean.py</verify>
  <acceptance_criteria>
    - 至少 2 条新正则与 jqxs/gctxt 相关（grep 可证）
  </acceptance_criteria>
  <done>水印表更新</done>
</task>

<task type="auto">
  <name>T2: 单测与防误伤</name>
  <files>tests/test_text_clean.py</files>
  <read_first>tests/test_text_clean.py</read_first>
  <action>
1. 新增合成用例：含 `jqxs ⊙cc` 与 `gctxt点cc`（或 E2E 同构变体）经 `raw=False` 后关键噪声不可辨或子串消失。  
2. 新增/保留反例：叙事短句含「顶点」**非整行营销**（可延续 Phase 5 既有测）。  
3. 可选 `parametrize` 空白变体。  
  </action>
  <verify>pytest tests/test_text_clean.py -q</verify>
  <acceptance_criteria>
    - 本文件仍不 import `novel_downloader`
  </acceptance_criteria>
  <done>黄金串锁死</done>
</task>

<task type="auto">
  <name>T3: README 一句 + 全量 pytest</name>
  <files>README.md</files>
  <read_first>README.md</read_first>
  <action>
在「正文清洗（默认开启）」同段或紧接其下，用 **一句** 说明 raw 仍含「换行统一、去 U+FEFF（BOM 类）」、不做营销规则。不拉长整节超过约 2 行。然后 **根目录** `pytest`（无参）全绿。  
  </action>
  <verify>cd 仓库根 && pytest -q</verify>
  <acceptance_criteria>
    - `README.md` 中可见 `FEFF` 或 `BOM` 或 `U+FEFF` 之一与 raw 同段说明
  </acceptance_criteria>
  <done>文档与回归</done>
</task>

</tasks>

<verification>
- 根目录 `pytest` 退出 0
- 可选：`python3 -c` 对合成 apibi 串 spot-check，无 `jqxs`/噪声子串或符合断言
</verification>
