---
phase: 09-maintainability-diagnostics
plan: "09-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - novel_downloader.py
  - .planning/codebase/CONCERNS.md
  - .planning/codebase/ARCHITECTURE.md
  - .planning/codebase/CONVENTIONS.md
  - .cursor/rules/gsd.md
autonomous: true
requirements:
  - MAIN-01
user_setup: []
must_haves:
  truths:
    - 仓库中不存在对 `save_to_file` 的引用
    - 规划文档与 `.cursor/rules/gsd.md` 对「死代码 save_to_file」的表述与代码一致
---

<objective>
完成 **MAIN-01**：删除 `NovelDownloader.save_to_file` 整段；将 `.planning/codebase/CONCERNS.md`、`ARCHITECTURE.md`、`CONVENTIONS.md` 与 `.cursor/rules/gsd.md` 中关于「未使用 save_to_file」的条目**更新为已移除**或**删除误引导句**，与 `09-CONTEXT` D-01～D-03 一致。

Output: `python3 -m py_compile novel_downloader.py`；`rg save_to_file` 仅在规划历史或本 PLAN 中命中（或零命中）。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-maintainability-diagnostics/09-CONTEXT.md
@novel_downloader.py
@.planning/codebase/CONCERNS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.cursor/rules/gsd.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 外部脚本 / fork 调用了 `save_to_file` | 低 | 全仓库已检索无调用；若有用户私有脚本，见 BREAKING 于 README/CHANGELOG 一句（本 plan 不强制，可选） |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 删除 `save_to_file`</name>
  <files>novel_downloader.py</files>
  <read_first>novel_downloader.py（类体 `save_to_file` 段）</read_first>
  <action>
删除 `def save_to_file(self, file_path, chapter_title, content):` 方法及其 docstring 全文（约 `run` 前一段）。不改动 `run()` 写入循环。  
  </action>
  <verify>python3 -m py_compile novel_downloader.py</verify>
  <acceptance_criteria>
    - `! grep -q "def save_to_file" novel_downloader.py`
  </acceptance_criteria>
  <done>单写入路径</done>
</task>

<task type="auto">
  <name>T2: 更新文档与规则</name>
  <files>CONCERNS.md,ARCHITECTURE.md,CONVENTIONS.md,gsd.md</files>
  <read_first>各文件中含 save_to_file 的段落</read_first>
  <action>
1. `CONCERNS.md`：将「save_to_file 未使用」小节改为 **已解决** 简述（或删除原小节并加一行「历史：已移除，见 Phase 9」）。  
2. `ARCHITECTURE.md` / `CONVENTIONS.md`：删去「另有 save_to_file」类表述，或改为「章节写入由 `run()` 集中完成」。  
3. `.cursor/rules/gsd.md`：与 `CONVENTIONS` 同步，不保留过时 API 列表。  
  </action>
  <verify>rg -n "save_to_file" .planning .cursor || true</verify>
  <acceptance_criteria>
    - 误引导「当前仍存在 save_to_file」的句子已清理
  </acceptance_criteria>
  <done>文档与代码一致</done>
</task>

</tasks>

<verification>
- `pytest -q` 全绿
- `rg save_to_file` — 无代码命中；规划文件仅可保留本 phase 或历史说明
</verification>
