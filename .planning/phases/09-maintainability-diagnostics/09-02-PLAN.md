---
phase: 09-maintainability-diagnostics
plan: "09-02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - novel_downloader.py
  - README.md
autonomous: true
requirements:
  - MAIN-02
user_setup: []
must_haves:
  truths:
    - `chapters` 为空时仍 `sys.exit(1)`，且 stderr/stdout 含可操作建议与当前 URL/书号线索
  artifacts:
    - 可选 README 一句「无章节时见终端提示」
---

<objective>
完成 **MAIN-02**：在 `NovelDownloader.run()` 中当 `get_download_url()` 返回空列表时，**替换或包裹**现有一句「未找到章节…」，输出 **3～8 行**内可读说明（`09-CONTEXT` D-04～D-06）：含当前 **target_url**、从 URL 解析的 **书号**（若可得）、**apibi 是否曾启用** 的提示、可能原因与 **检查 BQUGE_API_BASE**、书号/URL 检查、网络/反爬。保持 **退出码 1**。

可选：在 `__init__` 或 `get_download_url` 内设置 `self._catalog_empty_context: str`（或等价）供 `run()` 拼接——实现择一，以可读输出为准。

**不新增** 必须 `import novel_downloader` 的联网测试；回归以 `pytest` 全绿 + 手工运行无效书号/URL 为验收。

Output: 用户能根据终端输出做下一步；CI 仍失败在 exit 1。
</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-maintainability-diagnostics/09-CONTEXT.md
@novel_downloader.py（`get_download_url`、`_get_chapters_from_page`、`run`）
@bqg_api.py
@README.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 提示泄露敏感路径 | 低 | 仅打印用户已输入的 `target_url` 与公开书号 |
| 输出过长 | 低 | 限制 3～8 行主段落 + 可选 `---` 分隔 |
</threat_model>

<tasks>

<task type="auto">
  <name>T1: 可诊断上下文（可选）与排版函数</name>
  <files>novel_downloader.py</files>
  <read_first>09-CONTEXT.md,`run` 中 `if not chapters` 块</read_first>
  <action>
1. 实现 `_format_empty_catalog_message(self) -> str` 或**在 `run()` 内** 局部函数，用 `self.target_url`、`self._use_api`、`self._api_book_id`（如存在）、`os.environ.get("BQUGE_API_BASE")` 拼出短列表式说明（中文）。  
2. 若 `get_download_url` 在多处 `return []`，**可选** 在空返回前设 `self._empty_catalog_from = "apibi"|"html"|"error"` 等，仅用于更准一句子原因——不强制。  
3. 保持 `if not chapters: ... sys.exit(1)`。  
  </action>
  <verify>python3 -m py_compile novel_downloader.py</verify>
  <acceptance_criteria>
    - 单测可不增加；`pytest` 全绿
  </acceptance_criteria>
  <done>空目录有指南</done>
</task>

<task type="auto">
  <name>T2: README 补一句（可选但推荐）</name>
  <files>README.md</files>
  <read_first>README.md 免责声明/使用 附近</read_first>
  <action>
在「未找到章节」或故障排查意群加 **1～2 行**：程序会打印书号/URL 与常见检查项；CI 在 exit 1 时失败。  
  </action>
  <verify>grep -E "未找到章节|书号|exit" README.md | head -3</verify>
  <acceptance_criteria>
    - 用户能发现「无章节时看终端」
  </acceptance_criteria>
  <done>文档与行为对齐</done>
</task>

</tasks>

<verification>
- `pytest -q` 退出 0
- 手工：`python3 novel_downloader.py 0` 或明显无效书号，确认多行提示后 exit 1（不强制自动化）
</verification>
