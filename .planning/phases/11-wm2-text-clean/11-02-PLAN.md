---
phase: 11-wm2-text-clean
plan: "11-02"
type: execute
wave: 2
depends_on:
  - "11-01"
files_modified:
  - text_clean.py
  - tests/test_text_clean.py
  - README.md
autonomous: true
requirements:
  - CLEAN-03
user_setup: []
must_haves:
  truths:
    - 默认清洗路径在合成串上进一步减少或保持已消除的类水印族；raw 仅换行+去 FEFF
    - 根目录 `pytest` 通过；`tests/test_text_clean.py` 无对 `novel_downloader` 的 import
  artifacts:
    - text_clean.py
    - tests/test_text_clean.py
---

<objective>
在 **`11-INVENTORY.md` 结论** 指导下完成 **CLEAN-03** 的**实现与单测**：

- 若盘点列出**可合入**的具体合成形态：在 `text_clean.py` 追加或收紧**行内/行级**规则（与 `11-CONTEXT` D-03/D-04 一致），并在 `tests/test_text_clean.py` 增加/参数化用例，含至少一条**防误伤**正例（叙事里含易混词则沿用 `test_vertex_in_sentence_not_removed` 思路）。
- 若盘点结论为 **「无新必改形态」**：本 plan 退化为**回归加固**：为既有 jqxs/gctxt/bqfun 等增加 **1～2 组** 变体单测（空白、全半角、可选 `parametrize`），**不**无依据扩大删除类；`text_clean.py` 可仅作注释/分组整理，**或** 零逻辑变更，但须在用例或 README 中**一句** 说明「CLEAN-03 在现有规则下以加固单测收束」。

Output: `python3 -m py_compile text_clean.py`；`pytest` 仓库根全量通过；若改用户可见行为则 `README.md` 中「正文清洗」相关句同步。

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/11-wm2-text-clean/11-INVENTORY.md
@.planning/phases/11-wm2-text-clean/11-CONTEXT.md
@text_clean.py
@tests/test_text_clean.py
@README.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 误删叙事句 | 高 | 新行滤须配对攻用例；行内只锚定固定推广子串（CONTEXT D-04） |
| raw 语义漂移 | 中 | 改 `clean_chapter_text` 任一分支后跑 `test_raw_*` 全通过 |
| 全仓回归失败 | 中 | 执行根目录 `pytest`，不仅 `test_text_clean` |

</threat_model>

<tasks>

<task type="auto">
  <name>T1: 读盘点并定修改集</name>
  <files>text_clean.py,tests/test_text_clean.py</files>
  <read_first>.planning/phases/11-wm2-text-clean/11-INVENTORY.md,.planning/phases/11-wm2-text-clean/11-CONTEXT.md,text_clean.py,tests/test_text_clean.py</read_first>
  <action>
1. 通读 `11-INVENTORY.md` 的「本阶段可合入」与「无新形态」结论，确定本任务是否**修改** `_INLINE_NOISE` / `_line_is_spam_line` / `clean_chapter_text` 任一处。
2. 若**无**代码修改：在 `text_clean.py` 顶部或 `clean_chapter_text` docstring **追加一行** 指向 Phase 11 / CLEAN-03 与 INVENTORY 日期（`2026-04-24` 可改为执行日），并跳转 T2 仅加测。
3. 若**有**代码修改：每条新正则须能在 `11-INVENTORY` 或本 plan 的 action 中引用**具体合成样例**字符串（禁止无样例的宽泛规则）。
  </action>
  <verify>grep -n "CLEAN-03\|Phase 11\|11-INVENTORY" text_clean.py README.md 2&gt;/dev/null | head -3</verify>
  <acceptance_criteria>
    - 若零逻辑变更：`text_clean.py` 或 `README` 中至少一处出现 `CLEAN-03` 或 `Phase 11` 或 `INVENTORY` 引用
    - 若有逻辑变更：`text_clean.py` 中可定位到新或改的 `re.`/`_line` 相关片段
  </acceptance_criteria>
  <done>修改集确定并落笔或注记</done>
</task>

<task type="auto">
  <name>T2: 单测与全量 pytest</name>
  <files>tests/test_text_clean.py</files>
  <read_first>tests/test_text_clean.py,text_clean.py,.planning/phases/11-wm2-text-clean/11-INVENTORY.md</read_first>
  <action>
1. 在 `test_text_clean.py` 增加**新用例**或**parametrize 扩展**（与 T1 决定一致）；至少包括一条「合法叙事不被删」的断言（可扩展现有 `test_vertex_in_sentence_not_removed` 或新函数）。
2. 仓库根执行：`python3 -m py_compile text_clean.py` 与 `pytest`（全量，非仅子文件）。
3. 若 README 中关于默认/raw 的句子与 D-05 冲突，**修 README** 一句，不扩写大段。
  </action>
  <verify>cd 仓库根 &amp;&amp; python3 -m py_compile text_clean.py &amp;&amp; pytest -q</verify>
  <acceptance_criteria>
    - `pytest` 退出码 0
    - `rg "import novel_downloader" tests/test_text_clean.py` 无匹配
  </acceptance_criteria>
  <done>CLEAN-03 可执行验收完成</done>
</task>

</tasks>
