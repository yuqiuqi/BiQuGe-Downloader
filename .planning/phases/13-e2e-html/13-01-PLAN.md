---
phase: 13-e2e-html
plan: "13-01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/13-e2e-html/13-RUN-RECORD.md
autonomous: true
requirements:
  - E2E-01
user_setup: []
must_haves:
  truths:
    - 存在 `13-RUN-RECORD.md`，含「路径 A / 路径 B」的**可复现命令或脚本**、**终端依据（关键词）**、**章数或失败原因**、**环境**（`BQUGE_API_BASE` 是否设、Python 简版）
  artifacts:
    - .planning/phases/13-e2e-html/13-RUN-RECORD.md
---

<objective>
在 **不修改** `novel_downloader` 主逻辑**的前提下，为 **E2E-01** 做**采数与复现预演**：对**同一书号**（见 `13-CONTEXT` D-01）各跑一次**可识别的 apibi 成功路径**与 **HTML/回退路径**，把命令、环境、日志关键词、**目录章数**（或 0 章原因）与可选的**第 1 章观测**记入 `13-RUN-RECORD.md`。

不强制跑完全书下载；若全本过长，**以目录级事实为主**（`get_download_url` 返回的列表长度，可通过下方「最小复现」或整段终端输出至「共发现 N 章」为止）。

不产出最终定稿 `E2E-*.md`（留给 `13-02-PLAN`）。

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-e2e-html/13-CONTEXT.md
@novel_downloader.py
@bqg_api.py
@url_input.py
@.planning/REQUIREMENTS.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 仅在一台机器上能复现、未记录 env | 中 | 显式写清 `echo $BQUGE_API_BASE` 与命令行 |
| 与 apibi 路径未真正区分的「假对照」 | 中 | 路径 A 须出现 apibi 典型日志；路径 B 须出现「未走 apibi / 回退 / HTML 解析」相关事实（见 D-00） |
| 贴版权正文 | 高 | 仅统计与 ≤3 行匿名化样例，见 D-04 |

</threat_model>

<tasks>

<task type="auto">
  <name>T1: 选书与路径设计</name>
  <files>.planning/phases/13-e2e-html/13-RUN-RECORD.md</files>
  <read_first>.planning/phases/13-e2e-html/13-CONTEXT.md,novel_downloader.py,bqg_api.py</read_first>
  <action>
1. 在 `13-RUN-RECORD.md` 顶部写**书号/归一化 URL**（推荐沿用 CONTEXT 的公开书号如 `3953`，或维护者现网可访问的书号）。
2. **路径 A（apibi）:** 在**默认/可达** apibi 前提（不设置或正常 `BQUGE_API_BASE`）下，用 `python novel_downloader.py <书号或URL>` 或**最小解释器片段**只调用 `NovelDownloader(u).get_download_url()` 并 `len(chapters)`，记录终端中 **`已连接 apibi 书库`** 与 **`API 目录共 N 章`**（或等价信息）。
3. **路径 B（HTML/回退）:** 按 `13-CONTEXT` **D-02** 择一**可写清**的触发，例如将 `export BQUGE_API_BASE=http://127.0.0.1:9/invalid`（路径形式以能令 `try_fetch_book_api` 失败、`_use_api` 不保持为真为准）**或** 等待自然「API 未返回章节目录，回退…」**若你能稳定触发**；跑**同一书号**的目录获取，记录 **`_get_chapters_from_page`** 路径下的 **N 章** 与关键日志。若 **N 为 0**，照实写**原因**（反爬/结构/网络），不伪造成功。
4. 若两路径**章数不同**，在记录中用一行话说明**可能原因**（API 与 HTML 列表来源差异等）；若相同，也注明。
5. 可选：对**同一路径**下第一个章 `get_chapter_content` 拉取 1 段，只记**行数/是否乱码/是否见典型水印族名**等摘要，**不**贴长正文。
  </action>
  <verify>test -f .planning/phases/13-e2e-html/13-RUN-RECORD.md &amp;&amp; grep -qE "路径|apibi|API|章|HTML|回退" .planning/phases/13-e2e-html/13-RUN-RECORD.md</verify>
  <acceptance_criteria>
    - `13-RUN-RECORD.md` 含**路径 A 与 路径 B** 两小节或等价表
    - 含**至少一条**可复现 shell 行或**可复制**的 Python 片段
    - 章数**或**明确失败/0 章原因
  </acceptance_criteria>
  <done>RUN-RECORD 已写入</done>
</task>

</tasks>
