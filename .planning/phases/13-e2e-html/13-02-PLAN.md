---
phase: 13-e2e-html
plan: "13-02"
type: execute
wave: 2
depends_on:
  - "13-01"
files_modified:
  - .planning/phases/13-e2e-html/E2E-2026-04-24.md
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/STATE.md
autonomous: true
requirements:
  - E2E-01
  - E2E-02
user_setup: []
must_haves:
  truths:
    - 存在定稿 `E2E-*.md`（日期可与执行日一致，不必强求文件名与下例相同），含 CONTEXT **D-05** 所列字段/表；E2E-02 在 README 或该文档中**可定位一句话入口**
    - 根目录 `pytest` 仍全绿；本 plan **不新增**必联网的自动化测试
  artifacts:
    - .planning/phases/13-e2e-html/E2E-2026-04-24.md
---

<objective>
基于 **`13-RUN-RECORD.md` 的事实**，撰写**对外可读**的 E2E 结论文档 `E2E-*.md`（v1.4 / Phase 13 主验收物），完成 **E2E-01**；并落实 **E2E-02**：在 `README` **或** 结论文档中给出一句话**复现/入口**；更新 `REQUIREMENTS.md` 追溯、必要时 `STATE.md`；落盘 **`13-SUMMARY.md`**（执行摘要，供 `complete-milestone` / UAT 引用）。

**默认不改** `novel_downloader`；若 13-01 已暴露确定性 bug，本 plan **仅**在报告记「issue/后续 phase」，或经维护者显式允许下做**最小**文档/注释补丁。

</objective>

<execution_context>
@$HOME/.cursor/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/13-e2e-html/13-CONTEXT.md
@.planning/phases/13-e2e-html/13-RUN-RECORD.md
@.planning/phases/999.1-apibi-watermark-bom/E2E-2026-04-23.md
@README.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
</context>

<threat_model>
| 威胁 | 严重度 | 缓解 |
|------|--------|------|
| 与 999.1 文档大段重复 | 中 | 仅对照一句差异 + 链到 999.1，避免复制全书摘段 |
| E2E-02 成冗长改 README | 低 | 优先一句 + 指回 `E2E-*.md` § |
| 追溯表未更 | 低 | 必须编辑 `REQUIREMENTS.md` trace 行与 checklist |

</threat_model>

<tasks>

<task type="auto">
  <name>T1: 撰写 E2E-*.md</name>
  <files>.planning/phases/13-e2e-html/E2E-2026-04-24.md</files>
  <read_first>.planning/phases/13-e2e-html/13-RUN-RECORD.md,.planning/phases/13-e2e-html/13-CONTEXT.md</read_first>
  <action>
1. 将 `E2E-2026-04-24.md` 作为**首版文件名**（若执行日不同，可改名并在本任务说明注记原文件名与最终文件名）。结构建议：**背景** → **复现环境** → **路径 A（apibi）** → **路径 B（HTML/回退）** → **对照表**（D-05）→ **与 999.1 E2E 的差异**（一句）→ **结论/后续 issue 候选**。
2. 全文遵守 **D-04** 正文长度约束；**链接** 到 `13-RUN-RECORD` 作原始采数可选项。
3. 若 13-01 有未完成项，在结论写 **「待网络/时间」** 与如何补跑。
  </action>
  <verify>test -f .planning/phases/13-e2e-html/E2E-2026-04-24.md</verify>
  <acceptance_criteria>
    - 存在至少一个 `E2E-*.md` 于 `13-e2e-html/`
    - 含**对照表**或等价项目符号列表覆盖 D-05 列出的字段
  </acceptance_criteria>
  <done>结论文档已落盘</done>
</task>

<task type="auto">
  <name>T2: E2E-02 与追溯</name>
  <files>README.md,.planning/REQUIREMENTS.md</files>
  <read_first>README.md,.planning/REQUIREMENTS.md,.planning/phases/13-e2e-html/E2E-2026-04-24.md</read_first>
  <action>
1. 审阅 `README`：若已有「apibi / 目录 / 回退」**足够**的说明，在 E2E 文增加节 **E2E-02: README 已覆盖，见 §…** 并将 **E2E-02** checkbox 关联说明。否则在 **README** 增**一句**指向 `E2E-*.md` 或**三步内**复现总览（与 CONTEXT D-06 一致）。
2. 更新 `REQUIREMENTS.md`：将 E2E-01、E2E-02 勾为 `[x]`（若分阶段交付则写注释「见 E2E 文档」）；Traceability 表 `Phase 13` **Complete**（或 In progress 若 T1 未齐）。
3. 可选：更新 `STATE.md` 与 `ROADMAP` Progress 行（Phase 13 Complete 待 execute 后做）。
  </action>
  <verify>grep -qE "E2E-2026|E2E-|13-e2e-html" README.md .planning/REQUIREMENTS.md</verify>
  <acceptance_criteria>
    - 读者可在 **README 或 E2E 定稿** 中 **10 秒内** 找到「如何本仓库语境下做 apibi vs HTML 对照」
  </acceptance_criteria>
  <done>README/需求追溯已处理</done>
</task>

<task type="auto">
  <name>T3: 13-SUMMARY.md</name>
  <files>.planning/phases/13-e2e-html/13-SUMMARY.md</files>
  <action>
写执行摘要：日期、交付物列表、`pytest` 结果行、与 `11-UAT` 关系一句（见 CONTEXT）。  
  </action>
  <verify>test -f .planning/phases/13-e2e-html/13-SUMMARY.md</verify>
  <acceptance_criteria>含 `E2E-01` 与结论文档路径</acceptance_criteria>
  <done>SUMMARY 已写入</done>
</task>

</tasks>
