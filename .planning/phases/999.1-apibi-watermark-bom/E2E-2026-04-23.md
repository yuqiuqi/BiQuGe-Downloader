# E2E 抽测记录（《领地》id=6094）

**时间:** 2026-04-23  
**环境:** 本地 `python3 novel_downloader.py 6094`（apibi API，10 线程）  
**目标:** 完整下载、无乱码、排版正确  

## 1. 完整性

| 检查项 | 结果 |
|--------|------|
| API 章数 | 32 |
| 文件中 `--------------------`（20 个 `-`）分隔条 | 32 |
| 文案「下载失败」「无法解析此章节内容」 | 0 次出现 |
| 程序退出码 | 0 |
| 耗时 | 约 1.9s |

**结论:** 可认定全书 32 章均已拉取并落盘，无整章失败占位。

## 2. 编码与「乱码」

| 检查项 | 结果 |
|--------|------|
| 文件以 UTF-8 解码 | 通过 |
| Unicode 替换字 U+FFFD | 0 |
| 典型拉丁乱码/双重编码（如 `Ã`、`æ` 混用） | 未观察到 |

**但:** 每章正文开头（紧接在分隔线之后）多出现 **U+FEFF**（BOM/零宽无断行空格，ZWNBSP）**共 32 次**，为源站/接口在 `txt` 字段前附加的字节。视觉上可能在行首多一个“空白”，**不属于传统乱码，但影响洁净度。**

## 3. 排版

| 检查项 | 结果 |
|--------|------|
| 文件开头是否以两个 `\n` 起笔 | 否（符合 Phase 5 对章首空行的约束） |
| 连续空行 | 最大约 3 行（在可接受范围） |
| 章序与 API 标题 | 与 `booklist` 一致（如首章标题为 `分卷阅读1` 等，属站源目录命名） |

## 4. 与「输出质量」目标差距（下期待办）

`text_clean` 目前主要针对 `bqfun ⊕cc` 等行内/行级规则。本抽测 apibi 正文中出现大量**其它**水印形态，**未被清洗**，例如：

- `jqxs ⊙cc`、数字与符号 `⊙`、`⊕` 组合（**粗测:** `jqxs` 约 60 处、`⊙` 约 196 处）
- `gctxt点cc` 类（`gctxt` 约 68 处；含「点cc」的片段约 187 处，与具体句式重叠计数）

**风险:** 若作者正文合法使用部分符号/拉丁缩写，加规则前需**抽样**避免误伤（建议以「域名/营销固定串」为锚，或提供 raw 与 golden 用例）。

---

**来源:** 自动化统计 + 人工抽查 `领地.txt` 前若干屏；`*.txt` 已 gitignore 未入库。
